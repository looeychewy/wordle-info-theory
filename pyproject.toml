[project]
name = "wordle-info-theory"
version = "2.0.0"
description = "Optimizing Wordle gameplay by way of information theory"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest>=9.0.3"
] # how write

[tool.pytest.ini_options]
pythonpath = ["."]  # Effectively replaces "python -m pytest"